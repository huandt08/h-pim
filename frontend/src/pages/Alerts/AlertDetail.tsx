import React from 'react';
import { Typography } from 'antd';

const { Title } = Typography;

const AlertDetail: React.FC = () => {
  return (
    <div>
      <Title level={2}>Alert Detail</Title>
      <p>Alert detail page coming soon...</p>
    </div>
  );
};

export default AlertDetail;